import email
import socket
import os
import time



class user:
    def __init__(self):
        self.user = username
        self.pass = password
        self.email = email
        self.user_limit = self_limit

    def security(self,user,email,password):

    def allot_space(self,allocation):
        os.mkdir('./{}'.format(self.user)
